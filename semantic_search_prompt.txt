### 🧠 Дополнение к ТЗ: Улучшение поиска мест по категориям

Необходимо реализовать интеллектуальный поиск по категориям для туристического Telegram-бота, который получает данные из Excel-файла (.xlsx) с колонками:
id, address, coordinate (в формате "POINT (44.003277 56.331576)"), description, title, category_id, url.

---

### 🎯 Цель улучшения
Сделать поиск более гибким: бот должен понимать широкий спектр пользовательских запросов, даже если они не совпадают напрямую с названиями категорий.
Например:
- Пользователь пишет: "история" → бот предлагает исторические здания, музеи, памятники.
- Пользователь пишет: "кофейни" → бот находит заведения с категориями вроде "кафе", "кофейня", "кафе-бейкери", "coffee".
- Пользователь пишет: "стрит-арт" → бот предлагает граффити, арт-объекты, современные инсталляции.

---

### 🧩 Требования к реализации
1. Добавить слой семантического поиска:
   - Использовать векторное представление текста (через sentence-transformers или аналогичные модели) для сравнения пользовательского запроса и описаний/названий мест.
   - При вводе запроса бот преобразует текст в вектор и ищет наиболее близкие совпадения по косинусному сходству.
   - Можно использовать предобученные модели (например, paraphrase-MiniLM-L6-v2, all-mpnet-base-v2).

2. Комбинировать поиск по ключевым словам и по смыслу:
   - Если пользователь ввёл короткое слово (например, "музеи"), сначала искать точные совпадения в title или category_id.
   - Если ничего не найдено, выполнять семантический поиск по description и title.

3. Добавить возможность частичных совпадений:
   - Использовать нечеткий поиск (например, через библиотеку fuzzywuzzy или rapidfuzz) для обработки опечаток и синонимов.

4. Добавить синонимический словарь (встроенный или динамический):
   Пример:
   synonyms = {
       "кофейни": ["кафе", "coffee", "кофе", "бары"],
       "музей": ["история", "галерея", "культура"],
       "стрит-арт": ["граффити", "арт", "инсталляция", "современное искусство"]
   }
   Если пользователь вводит слово, которое есть в ключах словаря — поиск ведется и по соответствующим синонимам.

5. Результаты поиска должны быть релевантно отсортированы:
   - Сначала показываются совпадения по точным ключевым словам,
   - затем по семантическому сходству,
   - затем по нечеткому поиску.

---

### 💬 Пример диалога
Пользователь:
> Хочу посмотреть что-то про историю города

Бот:
> Нашёл несколько исторических мест:
> 🏛 Музей Культуры — ул. Ленина 15
> 🏰 Старый Кремль — пл. Свободы
> 🕍 Памятник Первым Поселенцам — ул. Кирова 2

---

### 🧰 Технические рекомендации
- Использовать Python-библиотеки:
  - sentence-transformers
  - rapidfuzz (или fuzzywuzzy)
  - pandas (для работы с xlsx)
  - aiogram (для Telegram-бота)
- Добавить кеширование эмбеддингов для ускорения (например, сохранять в .pkl файл).
- Возможен вариант через OpenAI Embeddings API или local transformer модель (если оффлайн).
